import styled from 'styled-components'

const StyledGame = styled.div`
    width: 800px;
    height: 800px;
    background: #FFF;
    display: flex;
    flex-wrap: wrap;
    position: relative;
`

export default StyledGame