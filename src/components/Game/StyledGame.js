import styled from 'styled-components'

const StyledGame = styled.div`
    width: 700px;
    height: 700px;
    background: #FFF;
    display: flex;
    flex-wrap: wrap;
    position: relative;
`

export default StyledGame